name: ophth_board
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0-100.2.beta

dependencies:
  cloud_firestore: ^5.6.8
  firebase_auth: ^5.5.4
  firebase_core: ^3.13.1
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  intl: ^0.20.2
  path_provider: ^2.1.5
  syncfusion_flutter_pdf: ^29.2.7+1
  syncfusion_flutter_pdfviewer: ^29.2.7
  shared_preferences: ^2.5.3
  permission_handler: ^12.0.1
  device_info_plus: ^11.5.0
  share_plus: ^11.1.0
  collection: ^1.19.1
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  custom_lint: ^0.7.5
  riverpod_lint: ^2.6.5

flutter:
  uses-material-design: true

  assets:
    - assets/pdf_forms/form_template.pdf
    - assets/pdf_forms/resident_leave_request.pdf

